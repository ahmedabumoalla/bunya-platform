# تقرير إصلاح Migration 002 وسجل الـMigrations

## 1. سبب الفشل السابق

كانت `002_provider_contractor_onboarding.sql` قد استُبدلت بعد إنشائها بنسخة compatibility-only. وفي الوقت نفسه كانت إضافات onboarding منسوخة داخل 001 المحلية، ولذلك أخفى التعديل تعارضًا تاريخيًا بدل إصلاحه. عند استعادة 002 الأصلية ظهر تكرار `account_onboarding_deliveries` وpolicy الخاصة بها. أُعيدت 001 أيضًا byte-for-byte إلى نسختها التاريخية الموثوقة حتى يصبح التسلسل 001 ثم 002 صحيحًا.

## 2. مصادر النسخ الأصلية

- 001: commit `ead8ed8`، وهو رأس `main` و`origin/main` وقت الإصلاح. تحقق `git diff --exit-code` أن 001 مطابقة له.
- 002: لم توجد في أي commit أو في `origin/main`؛ كانت untracked منذ إنشائها. المصدر الموثوق هو سجل جلسة Codex التي أنشأتها أول مرة: `rollout-2026-08-04T18-37-35-019fcd6c-50ca-7273-b299-fb98312765b4.jsonl`، حدث `apply_patch` عند `2026-08-04T16:42:47.330Z`. استعيد النص الكامل من ذلك الحدث دون تخمين.
- لا يوجد commit hash خاص بـ002 لأنها لم تُضف إلى Git تاريخيًا.

## 3. Migration 002 بعد الاستعادة

- المسار: `supabase/migrations/002_provider_contractor_onboarding.sql`.
- عدد الأسطر: 584.
- SHA256: `25b48082c21e3f6454d669d5a3dfaff1ea139e9e6670b20d4c7c3203a1e9fd13`.
- Baseline المفترض: 001 من commit `ead8ed8` قبل إضافات onboarding.
- أعمدة `profiles`: `must_change_password` و`temporary_password_issued_at` و`temporary_password_expires_at` و`password_changed_at` مع constraint نافذة كلمة المرور المؤقتة.
- أعمدة الطلبات: `public_idempotency_key` للمزود والمقاول مع format constraints وunique partial indexes.
- الجدول: `account_onboarding_deliveries` مع حالات provisioning والتسليم، provider references، attempts، timestamps وخطأ sanitized؛ لا Password أو OTP أو message body.
- Indexes: onboarding status/application وidempotency للمزود والمقاول.
- Trigger: تحديث `updated_at` لحالة onboarding.
- RPCs: `finalize_provider_application_approval`، `finalize_contractor_application_approval`، `mark_onboarding_credentials_delivery`، `complete_temporary_password_change`.
- كل SECURITY DEFINER يستخدم `search_path` محددًا وآمنًا.
- RLS: مفعّل على onboarding، وpolicy القراءة للإدارة بصلاحية `reviews.manage`.
- Grants: provisioning والتسليم لـ`service_role` فقط؛ تغيير كلمة المرور لـ`authenticated`؛ revoke من PUBLIC/anon حسب الحساسية.
- Storage: bucket `join-applications` خاص، حد 10 MiB، MIME محصور في PDF/JPEG/PNG/WebP، دون anon upload policy.
- Backfill: المستخدمون الحاليون يحتفظون بـ`must_change_password=false`.
- Preflight: يتحقق من جداول baseline، nullable applicant profile، الإدارة والصلاحيات والتدقيق وoutbox، وغياب التطبيق الجزئي قبل أي DDL.

## 4. الفرق المستخرج وما نُقل

النسخة المعدلة المؤقتة حذفت عمليًا كل DDL/RPC/RLS/Grant من 002 واستبدلته بفحص compatibility فقط. لم يكن داخلها أي schema feature مطلوبة تستحق النقل. استُبعد هذا الحذف لأنه كان يلغي migration التاريخية بدل تقديم delta. نُقل الغرض الوحيد الصالح — التحقق من توافق الحالة النهائية — إلى Migration أمامية مستقلة دون نسخ Objects أو إعادة إنشائها.

## 5. Migration الأمامية الجديدة

- الاسم: `supabase/migrations/010_reconcile_onboarding_migration_history.sql`.
- الرقم التالي الفعلي: 010.
- عدد الأسطر: 56.
- SHA256: `16f8e311481e0b38fbfff70a031f1f516af40fb86c7abe735ada6f18516dc92f`.
- Forward-only وداخل transaction.
- لا Tables/Columns/Indexes/Functions/Triggers/Policies جديدة؛ الـFinal Delta الوظيفي كان صفرًا.
- تنفذ preflight reconciliation فقط: الأعمدة والجدول وتواقيع RPC وRLS وخصوصية bucket وعدم منح provisioning RPCs إلى PUBLIC.
- لا DROP، لا حذف بيانات، لا secrets، لا OTP، لا passwords، ولا تكرار Objects موجودة في 002–009.

## 6. حماية التاريخ

- أضيف `scripts/check-migration-hashes.mjs` و`scripts/migration-hashes.json`.
- أضيف script: `npm run test:migration-hashes`.
- الحارس يتحقق من مجموعة الملفات وأسماء وترتيب 001–010 وSHA256 لكل ملف، ويفشل إذا تغير أي migration تاريخية أو اختفت.
- النتيجة: ناجح — 10 hashes متطابقة، ومنها 002 الأصلية و010 الجديدة.

## 7. التحقق من SQL

النتيجة الصادقة: **STATIC VALIDATION ONLY**.

- `psql`: غير متوفر.
- PostgreSQL service: غير متوفر.
- Supabase CLI: غير متوفر.
- local `npx supabase`: غير مثبت؛ لم يُثبت أو يُنفذ عن بعد.
- Docker: غير متوفر.
- لم تُطبق migrations على قاعدة محلية أو Production، ولذلك لا توجد نتيجة تطبيق أول/ثانٍ أو RPC/RLS runtime يمكن ادعاؤها.
- Static Validator: ناجح — 10 migrations، 147 tables، 93 functions، 335 policies.
- تحقق من الترقيم، ترتيب الجداول والأعمدة، FK، indexes، RPC calls/signatures، triggers/functions، RLS/policies/grants، SECURITY DEFINER/search_path، revoke، duplicate objects والمراجع المفقودة.
- اختبارات العقود: ناجحة للملكية وauthorization وidempotency وانتقالات الحالة وrollback locks وretry/dead-letter ومنع persistence للبيانات الحساسة.

## 8. نتائج فحوص المشروع

- Migration hash guard: ناجح.
- TypeScript: ناجح — `npx tsc --noEmit --pretty false --incremental false`.
- ESLint: ناجح — `npm run lint`.
- Build: ناجح — Next.js 16.2.9، وتم توليد 68 صفحة static بنجاح.
- Integration contracts: ناجح.
- Mock/localStorage guard: ناجح.
- Route audit: ناجح — 71 صفحة و11 handler و5 بوابات أدوار.
- Route smoke على production build المحلي: ناجح.
- `git diff --check`: ناجح؛ تحذيرات LF/CRLF فقط دون whitespace errors.

## 9. Git النهائي

- `git diff --stat`: 79 ملفًا متتبعًا متأثرًا من الجولات السابقة والحالية؛ 312 إضافة و4676 حذفًا. لا يشمل الملفات untracked في الإحصاء.
- `git status --short`: الشجرة غير نظيفة بسبب تغييرات الجولات السابقة، والـmigrations 002–010 والاختبارات/scripts الجديدة ما زالت untracked.
- `git diff -- supabase/migrations/001*`: فارغ؛ 001 عادت إلى commit `ead8ed8`.
- 002 مطابقة للمصدر الأصلي وفق SHA256 المحفوظ، ولا يظهر لها tracked diff لأنها لم تكن committed أصلًا.
- Migration الجديدة الوحيدة في هذه الجولة هي 010.
- لا يوجد `.env` حقيقي tracked؛ الموجود المعدل هو `.env.example` فقط.
- أزيلت ملفات سجل `tmp` المؤقتة من الناتج النهائي.
- لم يُنفذ `git add` أو commit أو push أو reset أو checkout. استُخدمت أوامر Git للقراءة والتحقق فقط.

## 10. التطبيق المقترح على Staging

بعد ضبط مشروع Staging والتحقق من ارتباطه، الأمر المقترح دون تنفيذه هنا:

```powershell
npx supabase db push --linked --include-all
```

ويُفضّل قبله تشغيل الأمر نفسه مع `--dry-run` في بيئة تتوفر فيها Supabase CLI.

استعلامات التحقق بعد التطبيق:

```sql
select version from supabase_migrations.schema_migrations order by version;
select column_name, is_nullable, column_default
from information_schema.columns
where table_schema='public' and table_name='profiles'
  and column_name in ('must_change_password','temporary_password_issued_at','temporary_password_expires_at','password_changed_at');
select relrowsecurity from pg_class where oid='public.account_onboarding_deliveries'::regclass;
select id, public, file_size_limit, allowed_mime_types from storage.buckets where id='join-applications';
select to_regprocedure('public.finalize_provider_application_approval(uuid,uuid,uuid,text)'),
       to_regprocedure('public.finalize_contractor_application_approval(uuid,uuid,uuid,text)'),
       to_regprocedure('public.mark_onboarding_credentials_delivery(text,uuid,text,text,text,text,text)'),
       to_regprocedure('public.complete_temporary_password_change()');
select p.proname, p.prosecdef, p.proconfig, p.proacl
from pg_proc p
where p.oid in (
  'public.finalize_provider_application_approval(uuid,uuid,uuid,text)'::regprocedure,
  'public.finalize_contractor_application_approval(uuid,uuid,uuid,text)'::regprocedure,
  'public.mark_onboarding_credentials_delivery(text,uuid,text,text,text,text,text)'::regprocedure,
  'public.complete_temporary_password_change()'::regprocedure
);
```

## 11. التأكيدات

- 001 و002 عادتا إلى المصدرين التاريخيين الموثوقين ولم تُعدلا بعد الاستعادة.
- الإصلاح الأمامي موجود في 010 ولا يحتاج المستخدم إلى نسخ SQL أو إنشاء ملف يدويًا.
- لا توجد RPC مستخدمة بلا Function وفق الفحص الساكن.
- لا توجد مراجع Table/Column مفقودة ضمن الفحص.
- لم يُنفذ SQL Remote أو أي تعديل على Production.
- لم تُنفذ Git actions تغيّر repository أو remote.
- لم تُخزن Secrets أو OTP أو Passwords.

MIGRATION HISTORY REPAIR: PASSED

# تقرير E2E المتصل — 2026-08-13

- Run ID: `bunya-e2e-20260812-234714-875a62`.
- تم إنشاء مستخدمين مؤقتين للأدوار admin/customer/provider/contractor/driver مع peer fixtures لفحوص RLS، ونجح تسجيل الدخول الحقيقي لكل دور عبر Supabase sessions وصفحات البوابات.
- نجح OTP Hook محليًا: valid/invalid signature، GREEN-API mock، Resend copy policy، وعدم تسريب OTP في الاستجابة أو سجلات provider submissions. فحص التسليم الخارجي ما زال Pending.
- نجحت دورة التجارة المتصلة حتى Delivered باستخدام fallback fixtures بعد ثبوت أن Migration 011 غير مطبقة Remote؛ مسار RFQ RPC الأصلي ما زال محجوبًا بخطأ enum 42804.
- نجحت خدمات المقاول create/submit/needs_changes/resubmit/approve وreject، ونجح Portfolio workflow والتخزين، لكن القراءة العامة بعد approval فشلت بسبب RLS Remote؛ أضيفت Migration 014 محليًا فقط.
- نُفذت فرص/عروض/مشاريع/milestones والدعم والتسويات وRLS، مع blockers في project request enum وproposal resubmit/accept على Remote؛ أضيفت Migrations 015 و016 محليًا فقط.
- نُفذت اختبارات الإشعارات WhatsApp و400/429/500/dead-letter وعدم استخدام Resend للأحداث التشغيلية؛ ظهر فقد recipient notification رغم معالجة الحدث.
- Cleanup النهائي فشل لأن Remote لا يحتوي إصلاحات immutable referential cleanup؛ بقيت audit/financial/settlement/project/profile/auth records. أضيفت Migration 017 محليًا فقط، ولم تُطبق أي Migration Remote.
- Migrations المحلية الجديدة في الجولة: 013–017. لم تُعدّل أي Migration تاريخية، ولم ينفذ commit/push.
- التحقق النهائي: TypeScript PASS، ESLint PASS، Build PASS، Integration PASS، SQL static PASS (17 migrations, 147 tables, 96 functions, 335 policies)، git diff --check PASS مع تحذير LF/CRLF فقط.
- النتيجة: `REAL E2E: FAILED` بسبب Remote schema blockers وCleanup غير مكتمل.

## 12. Supabase Remote sync attempt — 2026-08-13

- Scope was limited to the existing project at `C:\Projects\bunya-platform`; no repository, Git remote, or unrelated project was touched.
- The only project ref available in the workspace is `ccvbtduzkvuzvckfwqik`, derived from the configured Supabase URL. It matches the ref used by the previous connected E2E run; no conflicting linked ref or `supabase/config.toml` exists.
- Local migration inventory is 001–017. Historical migrations 001–010 were left unchanged. The forward-only candidates are 011–017, in numeric order.
- Remote behavior from the prior connected run confirms that 011, 012, 014, 015, 016, and 017 are absent. Migration 013 is also local-only and belongs in the same ordered forward batch.
- A workspace-local ephemeral Supabase CLI 2.114.0 was used only for inspection. Remote project and migration-history inspection could not authenticate because `SUPABASE_ACCESS_TOKEN` is not configured.
- The configured Supabase service secret works for Auth Admin and PostgREST data access, but it is not a Supabase Management API token and received HTTP 401 for database-query access. The `supabase_migrations` schema is not exposed through PostgREST (PGRST106), and the public RPC surface contains no SQL/DDL executor.
- Therefore no safe supported channel was available to inspect authoritative remote migration history or execute DDL. No migration was applied, altered, replayed, or created.
- Because the required forward migrations were not synchronized, the previously failing E2E slices were not rerun against the known-stale schema. This avoids generating another E2E run and more residual data without changing the blocker.
- Cleanup was not retried because the missing immutable-referential-cleanup migrations are the direct reason it failed previously. No broad delete or remote reset was attempted.
- Read-only residual-data verification for run `bunya-e2e-20260812-234714-875a62` currently finds: 20 Auth users, 20 profiles, 16 contractor profiles, 4 project requests, 8 settlement requests, and 8 contractor financial transactions. Remaining E2E data is therefore YES.
- Final checks: TypeScript PASS, ESLint PASS, Build PASS, and `git diff --check` is recorded separately after this report update. Build emitted only the existing Node.js 20 deprecation warning from Supabase JS.
- Required unblocker: provide a Supabase CLI access token for this account, or a direct database connection credential/password for project `ccvbtduzkvuzvckfwqik`. The existing service secret cannot apply migrations.
- Result of this sync attempt: `REAL E2E: FAILED`.

## 13. Manual Supabase reconciliation migration — 2026-08-13

- أُنشئ ملف Forward Migration واحد جديد: `supabase/migrations/018_remote_e2e_reconciliation.sql`. الرقم التالي الفعلي بعد 017 هو 018.
- الملف جاهز كدفعة واحدة لـSupabase SQL Editor؛ يبدأ بـ`begin;` وينتهي بـ`commit;`. لم يُنفّذ أي SQL على Remote، ولم يُستخدم Supabase CLI أو Access Token أو DB Password.
- حجم الملف 1344 سطرًا، وSHA256 هو `0d60c107a405d4f69c5a9e6718ff3cdf1ca018eb67f71e21dd07696937581295`.
- الإصلاح التطبيقي Resend Mock Endpoint موجود في `src/lib/notifications/providers/resend.ts`: يستخدم `RESEND_API_URL` عند ضبطه، مع بقاء endpoint الإنتاجي الافتراضي. لا يوجد له DDL داخل Migration لأنه ليس كائن قاعدة بيانات.
- RFQ Enum Assignment: أُعيد تعريف `submit_customer_rfq(jsonb,jsonb,text)` مع casts صريحة إلى القيم الحالية `sourcing`/`verifying` و`comparing_prices`/`verifying_availability` بعد preflight على الأنواع والقيم.
- Project Request Enum Assignment: أُعيد تعريف `submit_customer_project_request(jsonb,text[],text)` مع cast صريح لـ`new` إلى `contractor_opportunity_status` و`receiving_proposals` إلى `project_request_lifecycle_status`؛ لم تُضف أي Enum value جديدة.
- Catalog Outbox Collision: أصبحت مفاتيح إرسال الخدمات وPortfolio محددة بالكيان ورقم دورة المراجعة، ومفاتيح قرار المراجعة مرتبطة بمعرف audit الفعلي. أزيل trigger العشوائي المؤقت إن كان موجودًا.
- Proposal Resubmit Collision: أصبحت `save_contractor_proposal(uuid,jsonb,jsonb,boolean,text)` تستخدم `p_idempotency_key` فعليًا عبر `contractor_workflow_idempotency`، وتفصل resubmit عن العملية الأولى مع منع event مكرر لنفس العملية.
- Portfolio Public RLS: صُححت policy بتأهيل `contractor_portfolio_items.profile_id` صراحة داخل correlated subquery. سياسات الخدمات والمشاريع والعروض والمراحل والإشعارات والدعم والتسويات روجعت ولم يحتج المسار المثبت إلى تغيير إضافي.
- Notifications: استُبدل partial unique index على `notifications.event_key` بـnullable full unique index قابل للاستنتاج من PostgREST upsert؛ يوجد preflight يمنع التنفيذ إن وُجد تعارض حقيقي.
- Immutable Referential Cleanup: أصبح FK الخاص بـ`project_audit_logs.project_request_id` يستخدم `ON DELETE SET NULL` مع بقاء audit immutable. أضيفت حواجز حذف مالية خاصة تسمح فقط لـservice role وبروفايلات E2E المؤكدة عبر metadata والبريد المحجوز، ولا تسمح بحذف سجلات المستخدمين الحقيقيين.
- أضيف RPC اختياري `cleanup_e2e_immutable_records(text)` لا يُنفذ أثناء Migration، ومقيد بـservice role ومعرف `bunya-e2e-*` مطابق لحساب Auth E2E. لا يوجد CASCADE عام أو حذف بيانات أو تعديل UUID.
- قبل أي GRANT/REVOKE جديد، تفحص Migration التوقيع الحقيقي عبر `pg_proc` و`pg_get_function_identity_arguments` و`to_regprocedure`; لا يوجد اعتماد على `public.app_role` أو نوع مفترض.
- Preflight يغطي العلاقات والأعمدة والتواقيع والـEnums المطلوبة. لا يوجد `DROP TABLE` أو `TRUNCATE` أو `db reset` أو تخزين OTP/Password/Secret.
- `npm run test:sql`: PASS — 18 migrations، 147 tables، 102 functions، 335 policies.
- Hashes الخاصة بـ001–010 مطابقة للـmanifest ولم تتغير. لم تُعدّل 011–017 في هذه الجولة، ولم تُنشأ Migration أخرى.
- TypeScript: PASS. ESLint: PASS. Build: PASS (مع تحذير Node.js 20 deprecation الحالي فقط). لم يُشغّل E2E حسب الطلب.
- لم يُنفذ commit أو push. نتيجة `git diff --check` تُسجل بعد تحديث هذا التقرير.

MANUAL REMOTE MIGRATION: READY

## 14. Failed-slice rerun after Remote Migration 018 — 2026-08-13

- Remote detection: PASS. The new `is_e2e_test_profile(uuid,text)` RPC from 018 is callable through the connected service context, proving that 018 is present on the target project.
- Old cleanup: PASS. Removed the two known residual runs `bunya-e2e-20260812-234714-875a62` and `bunya-e2e-20260810-040333-shyrej`: 21 Auth E2E users plus their known profiles, applications, contractor records, projects, finance, settlements, audit rows, notifications/outbox, and dependent rows. No real-user selector or broad database deletion was used.
- The approved E2E cleanup guard in 018 was used for immutable audit rows. Orphaned approved contractor profiles were removed before their E2E applications so the existing approval-source constraints stayed valid.
- New run ID: `bunya-e2e-20260813-013500-c8f4d1`. Runtime passwords, OTP values, and secrets were neither stored nor printed.
- OTP Hook smoke: PASS. Invalid/valid signatures and mocked provider routing completed before the later commerce failure.
- Login smoke: PASS for admin/customer/provider/contractor/driver. RLS critical smoke: PASS for the pre-commerce isolation checks.
- Commerce regression reached real customer RFQ, provider targeting, provider response, availability/delivery confirmations, and best-price selection. The enum error fixed by 018 did not recur.
- Commerce then failed at `assemble_bunya_customer_quote(uuid)`: it returned NULL because migration 005 updates `internal_sourcing_requests.completed_at`, but that column is absent from both the historical table definition and Remote. The function's broad exception handler masks this database error as NULL.
- The missing-column cause was confirmed independently: direct schema-cache access to `completed_at` returns PGRST204, while quote and quote-item inserts themselves succeed. Temporary diagnostic operations were removed from the final harness.
- A new forward-only fix was created without changing history: `supabase/migrations/019_add_internal_sourcing_completion_timestamp.sql`. It adds only nullable `completed_at timestamptz` with table/function/type preflight checks. It was not applied remotely.
- Because 019 is not on Remote, Commerce is FAIL and the dependent Portfolio, contractor-project, Notifications, contractor-services, Support, and Finance/settlements slices were not continued. Repeating them against the known blocker would only create more temporary data.
- Final cleanup: PASS. All data from the diagnostic attempts and the new run was removed. Final connected scan: 0 E2E Auth users, applications, contractor profiles, RFQs, project requests, support, finance, settlements, notifications, outbox rows, and matching storage objects.
- Static SQL validation: PASS — 19 migrations, 147 tables, 102 functions, 335 policies.
- TypeScript: PASS. ESLint: PASS. Build: PASS. Build emitted only the existing Node.js 20/Supabase JS deprecation warning.
- No historical migration was changed. No Remote SQL, Git commit, or push was executed.
- Current blocker: manually apply Migration 019 to the same Supabase project, then rerun only Commerce and the not-yet-reached dependent slices.

REAL E2E: FAILED

## 15. Failed-slice rerun after Remote Migration 019 — 2026-08-13

- Remote detection: PASS. A connected PostgREST projection of `internal_sourcing_requests.completed_at` returned HTTP 200, confirming that Migration 019 is present in the target schema cache.
- New connected run IDs were isolated with `bunya-e2e-*` Auth metadata. Runtime passwords, OTP values, and secrets were not stored or printed.
- Login smoke: PASS for all required roles. OTP Hook smoke: PASS for valid/invalid signatures and provider-copy policy. RLS critical smoke: PASS.
- Commerce: FAIL. RFQ creation, provider targeting/response, enum assignments, selection, order/invoice/payment, and signed payment processing were exercised. The real quote assembly RPC still fails because it attempts the forbidden `sourcing -> quote_ready` transition without `verifying`. Driver assignment also mutates protected `assigned_at`, and delivery status propagation assigns a text CASE result to `provider_order_status`.
- Portfolio: PASS, including media upload, needs_changes/resubmit/approve, independent reject, public approved-visible read, and private-item RLS.
- Contractor services: PASS for create/submit/needs_changes/resubmit/approve.
- Contractor project flow: FAIL at real proposal acceptance. Request/opportunity/proposal/resubmit worked; the acceptance RPC inserts milestones under the customer context and the milestone guard rejects that trusted initial insert. A diagnostic service-context continuation confirmed milestone sequencing and 100% progress behavior.
- Support: PASS for customer ticket, admin reply, resolution, and visibility isolation. Finance/settlements: PASS for balance, bank account, reject/approve/processing/paid transitions, state validation, and immutability smoke.
- Notifications: PASS for the events reached in this run, including recipient/template/action URL, idempotency, provider submission, retry classification, max attempts, dead-letter, and duplicate prevention.
- Forward-only Migration 020 was created locally: `020_fix_remaining_connected_workflow_transitions.sql`. It fixes quote transition sequencing, driver assignment, delivery order enum assignment, proposal-acceptance milestone initialization, and verified pending-payment E2E cleanup. No historical migration was changed and 020 was not applied remotely.
- Harness cleanup now includes `idempotency_keys` by E2E profile. Final cleanup was completed manually with exact verified E2E identities and relationship-derived IDs only; no real-user selector or broad reset was used.
- Final connected scan: 0 verified E2E Auth users and 0 marker rows across profiles, applications, RFQs, project requests, services, portfolio, support, settlements, products, and categories.
- SQL static validation: PASS — 20 migrations, 147 tables, 102 functions, 335 policies. No Remote SQL, Git commit, or push was executed.
- Final checks: TypeScript PASS, ESLint PASS, Build PASS, and `git diff --check` PASS. Build emitted only the existing Node.js 20 deprecation warning from Supabase JS.
- Current blocker: Migration 020 must be applied manually before the three genuine failing Remote paths can be rerun.

REAL E2E: FAILED
